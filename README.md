This is a small project to test the AI-version of VSCODE.  The task was
"I have a command-line program called focus stack.  I want to run it in a docker
container so that it can be used through a browser interface. As first step provide
me with server code that runs the program and provides user with an API."

Running focus-stack in container appears to be difficult. I tried a few iterations. Aaybe I remove that later. 

AI generated the GUI pretty nicely.
